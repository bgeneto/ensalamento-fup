# Reflex Framework - Production Dependencies
reflex==0.8.19

# Database and ORM (SQLite3 as in legacy system)
sqlalchemy>=2.0.0

# Async support (built into reflex in most cases, but explicit for clarity)
asyncio
uvloop

# Date/Time handling
python-dateutil

# PDF generation (same as legacy)
reportlab>=4.0.0
pytz

# Font support for PDFs
Pillow>=8.0.0
fonttools

# HTTP client for API calls
httpx>=0.24.0

# Excel/PDF exports
openpyxl>=3.0.0

# Environment management
python-dotenv

# Data validation and serialization
pydantic>=2.0.0
typing-extensions>=4.5.0

# For PDF unicode fonts (if needed)
unicodecsv
